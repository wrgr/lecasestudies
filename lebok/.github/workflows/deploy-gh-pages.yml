# Builds the Astro site and publishes dist/ to the gh-pages branch.
#
# NOTE: GitHub only runs workflows found in `.github/workflows/` at the REPOSITORY
# ROOT. While lebok lives as a subfolder of lens-concentration this file is inert;
# it activates once lebok is its own repo (see README "Splitting into its own repo").
# In repo Settings -> Pages, set the source to the gh-pages branch after the first run.
name: Deploy Astro site to gh-pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: gh-pages-deploy
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build Astro site
        run: npm run build

      - name: Deploy to gh-pages branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: ./dist
          force_orphan: true
          enable_jekyll: false
          # Uncomment and set once a custom domain is chosen; must match public/CNAME.
          # cname: lebok.example.org
